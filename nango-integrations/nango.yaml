integrations:
    outlook:
        syncs:
            emails:
                description: |
                    🔧 V18 FINAL FIX - Removed $select entirely, fetch all attachment metadata!
                    Only checks attachments if hasAttachments=true. Downloads via $value endpoint.
                    1-hour lookback for fast testing. Send test emails with PDFs to verify.
                    Uses Exchange FullAccess delegation. Mail.ReadWrite scope required.
                runs: every hour
                output: OutlookEmail
                sync_type: incremental
                endpoint:
                    method: GET
                    path: /emails
                    group: Mail
                scopes: offline_access Mail.ReadWrite Mail.Read.Shared User.Read

    # gmail:
    #     syncs:
    #         emails:
    #             description: |
    #                 🔍 V1 GMAIL SYNC - Enhanced debugging for Gmail attachment detection!
    #                 Logs hasAttachments detection, CID references in body, Gmail API responses, attachment details.
    #                 Downloads attachments as base64 via Gmail API. Default lookback is 1 week (configurable via backfillPeriodMs metadata).
    #             runs: every hour
    #             output: GmailEmail
    #             sync_type: incremental
    #             endpoint:
    #                 method: GET
    #                 path: /emails
    #                 group: Mail
    #             scopes: https://www.googleapis.com/auth/gmail.readonly

    # google-drive:
    #     syncs:
    #         all-files:
    #             description: |
    #                 Syncs ALL files from Google Drive (auto-runs every hour).
    #                 Returns metadata only - use fetch-document action to download content.
    #             runs: every hour
    #             output: DriveFile
    #             sync_type: incremental
    #             endpoint:
    #                 method: GET
    #                 path: /all-files
    #                 group: Documents
    #             scopes: https://www.googleapis.com/auth/drive.readonly
    #     actions:
    #         fetch-document:
    #             input: IdEntity
    #             description: |
    #                 Fetches the content of a file given its ID and returns it as base64.
    #                 For Google Workspace files (Docs, Sheets, Slides), these are exported to PDF/Excel format.
    #                 Use this to download PDFs, Word docs, images, and any other file from Drive.
    #             output: string
    #             version: 1.0.0
    #             endpoint:
    #                 method: GET
    #                 path: /fetch-document
    #                 group: Documents
    #             scopes: https://www.googleapis.com/auth/drive.readonly

models:
    IdEntity:
        id: string
    
    Attachments:
        filename: string
        mimeType: string
        size: number
        attachmentId: string
    
    OutlookEmail:
        id: string
        sender: string | undefined
        recipients: string | undefined
        date: string
        subject: string
        body: string
        attachments: Attachments[]
        threadId: string
    
    GmailAttachment:
        attachmentId: string
        filename: string
        mimeType: string
        size: number
        contentBytes: string | undefined
    
    GmailEmail:
        id: string
        sender: string
        recipients: string
        date: string
        subject: string
        body: string
        attachments: GmailAttachment[]
        threadId: string
    
    DriveFile:
        id: string
        name: string
        mimeType: string
        webViewLink: string | undefined
        parents: string[] | undefined
        modifiedTime: string
        createdTime: string
        size: number | undefined
        trashed: boolean
